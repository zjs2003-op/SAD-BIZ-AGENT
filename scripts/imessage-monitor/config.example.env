API_URL=https://your-app.vercel.app

# Background scheduler interval (seconds). Each run imports max 5 msgs then exits.
POLL_INTERVAL_SECONDS=180

LOOKBACK_DAYS=14

# Hard-capped at 5 in code
MAX_IMPORTS_PER_CYCLE=5

CHAT_FILTER=
IMPORT_FROM_ME=true
IMPORT_FROM_OTHERS=true
